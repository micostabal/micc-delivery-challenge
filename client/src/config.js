
const BASE_URL = "https://micc-delivery-challenge-server.herokuapp.com";

const config = {
  endpoints: {
    DISTANCE_REQUEST: `${BASE_URL}/distance-request`,
    ENTRIES: `${BASE_URL}/entries`
  }
};

export default config;